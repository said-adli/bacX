export default function LoginPage() {
    return (
        <div className="min-h-screen flex items-center justify-center">
            <h1>Supabase Login (Pending Implementation)</h1>
        </div>
    );
}
